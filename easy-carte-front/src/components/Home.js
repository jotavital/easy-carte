import { Typography } from "@mui/material";
import { Grid } from "@mui/material";

function Home() {
    return (
        <Grid container>
            <Typography variant="h2">
                Home.js
            </Typography>
        </Grid>
    );
}

export default Home;